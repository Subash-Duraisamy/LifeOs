
 /* ==========================================================
   FLOATING PARTICLES
========================================================== */

.streak-popup::before{

    content:"";

    position:absolute;

    inset:0;

    pointer-events:none;

    background:

        radial-gradient(circle at 15% 18%, rgba(255,255,255,.22) 0 2px, transparent 3px),

        radial-gradient(circle at 80% 25%, rgba(255,220,120,.35) 0 2px, transparent 3px),

        radial-gradient(circle at 30% 70%, rgba(110,180,255,.28) 0 2px, transparent 3px),

        radial-gradient(circle at 70% 80%, rgba(255,255,255,.18) 0 2px, transparent 3px),

        radial-gradient(circle at 90% 60%, rgba(255,180,70,.28) 0 2px, transparent 3px),

        radial-gradient(circle at 12% 88%, rgba(160,120,255,.25) 0 2px, transparent 3px);

    animation:

        sparkleMove 18s linear infinite;

}

/* ==========================================================
   SECOND LAYER
========================================================== */

.streak-popup::after{

    content:"";

    position:absolute;

    inset:0;

    pointer-events:none;

    opacity:.55;

    background:

        radial-gradient(circle at 20% 35%, rgba(255,255,255,.12) 0 1px, transparent 2px),

        radial-gradient(circle at 50% 10%, rgba(255,210,80,.20) 0 1px, transparent 2px),

        radial-gradient(circle at 85% 45%, rgba(120,180,255,.18) 0 1px, transparent 2px),

        radial-gradient(circle at 40% 92%, rgba(255,255,255,.16) 0 1px, transparent 2px),

        radial-gradient(circle at 65% 68%, rgba(255,150,70,.22) 0 1px, transparent 2px);

    animation:

        sparkleMoveReverse 22s linear infinite;

}

/* ==========================================================
   SPARKLE MOTION
========================================================== */

@keyframes sparkleMove{

    from{

        transform:

            translateY(0)

            translateX(0);

    }

    to{

        transform:

            translateY(-90px)

            translateX(18px);

    }

}

@keyframes sparkleMoveReverse{

    from{

        transform:

            translateY(0)

            translateX(0);

    }

    to{

        transform:

            translateY(90px)

            translateX(-18px);

    }

}

/* ==========================================================
   BACKGROUND LIGHT
========================================================== */

.streak-popup-glow::before{

    content:"";

    position:absolute;

    inset:-30%;

    border-radius:50%;

    background:

        radial-gradient(

            circle,

            rgba(255,215,80,.22),

            transparent 70%

        );

    filter:blur(80px);

    animation:

        pulseGlow 5s ease-in-out infinite;

}

@keyframes pulseGlow{

    0%{

        transform:scale(.8);

        opacity:.45;

    }

    50%{

        transform:scale(1.15);

        opacity:.9;

    }

    100%{

        transform:scale(.8);

        opacity:.45;

    }

}

/* ==========================================================
   PREMIUM GLASS BORDER
========================================================== */

.streak-popup{

    border:1px solid rgba(255,255,255,.08);

}

/* ==========================================================
   LIGHT REFLECTION
========================================================== */

.streak-popup{

    isolation:isolate;

}

.streak-popup .glass-shine{

    position:absolute;

    top:-30%;

    left:-45%;

    width:180px;

    height:800px;

    background:

        linear-gradient(

            to right,

            transparent,

            rgba(255,255,255,.12),

            transparent

        );

    transform:

        rotate(25deg);

    animation:

        shineMove 8s linear infinite;

    pointer-events:none;

}

@keyframes shineMove{

    from{

        left:-55%;

    }

    to{

        left:140%;

    }

}
/* ==========================================================
   FLOATING PARTICLES
========================================================== */

.streak-popup::before{

    content:"";

    position:absolute;

    inset:0;

    pointer-events:none;

    background:

        radial-gradient(circle at 15% 18%, rgba(255,255,255,.22) 0 2px, transparent 3px),

        radial-gradient(circle at 80% 25%, rgba(255,220,120,.35) 0 2px, transparent 3px),

        radial-gradient(circle at 30% 70%, rgba(110,180,255,.28) 0 2px, transparent 3px),

        radial-gradient(circle at 70% 80%, rgba(255,255,255,.18) 0 2px, transparent 3px),

        radial-gradient(circle at 90% 60%, rgba(255,180,70,.28) 0 2px, transparent 3px),

        radial-gradient(circle at 12% 88%, rgba(160,120,255,.25) 0 2px, transparent 3px);

    animation:

        sparkleMove 18s linear infinite;

}

/* ==========================================================
   SECOND LAYER
========================================================== */

.streak-popup::after{

    content:"";

    position:absolute;

    inset:0;

    pointer-events:none;

    opacity:.55;

    background:

        radial-gradient(circle at 20% 35%, rgba(255,255,255,.12) 0 1px, transparent 2px),

        radial-gradient(circle at 50% 10%, rgba(255,210,80,.20) 0 1px, transparent 2px),

        radial-gradient(circle at 85% 45%, rgba(120,180,255,.18) 0 1px, transparent 2px),

        radial-gradient(circle at 40% 92%, rgba(255,255,255,.16) 0 1px, transparent 2px),

        radial-gradient(circle at 65% 68%, rgba(255,150,70,.22) 0 1px, transparent 2px);

    animation:

        sparkleMoveReverse 22s linear infinite;

}

/* ==========================================================
   SPARKLE MOTION
========================================================== */

@keyframes sparkleMove{

    from{

        transform:

            translateY(0)

            translateX(0);

    }

    to{

        transform:

            translateY(-90px)

            translateX(18px);

    }

}

@keyframes sparkleMoveReverse{

    from{

        transform:

            translateY(0)

            translateX(0);

    }

    to{

        transform:

            translateY(90px)

            translateX(-18px);

    }

}

/* ==========================================================
   BACKGROUND LIGHT
========================================================== */

.streak-popup-glow::before{

    content:"";

    position:absolute;

    inset:-30%;

    border-radius:50%;

    background:

        radial-gradient(

            circle,

            rgba(255,215,80,.22),

            transparent 70%

        );

    filter:blur(80px);

    animation:

        pulseGlow 5s ease-in-out infinite;

}

@keyframes pulseGlow{

    0%{

        transform:scale(.8);

        opacity:.45;

    }

    50%{

        transform:scale(1.15);

        opacity:.9;

    }

    100%{

        transform:scale(.8);

        opacity:.45;

    }

}

/* ==========================================================
   PREMIUM GLASS BORDER
========================================================== */

.streak-popup{

    border:1px solid rgba(255,255,255,.08);

}

/* ==========================================================
   LIGHT REFLECTION
========================================================== */

.streak-popup{

    isolation:isolate;

}

.streak-popup .glass-shine{

    position:absolute;

    top:-30%;

    left:-45%;

    width:180px;

    height:800px;

    background:

        linear-gradient(

            to right,

            transparent,

            rgba(255,255,255,.12),

            transparent

        );

    transform:

        rotate(25deg);

    animation:

        shineMove 8s linear infinite;

    pointer-events:none;

}

@keyframes shineMove{

    from{

        left:-55%;

    }

    to{

        left:140%;

    }

}